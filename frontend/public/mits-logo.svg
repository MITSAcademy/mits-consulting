<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 600 240" fill="none">
  <!-- MITS wordmark — heavy sans-serif, uses currentColor so it adapts
       to dark sidebar OR light surfaces without needing two assets. -->
  <text x="300" y="170"
        font-family="'Inter','Helvetica Neue','Helvetica',Arial,sans-serif"
        font-size="180"
        font-weight="900"
        text-anchor="middle"
        letter-spacing="-2"
        fill="currentColor">MITS</text>
</svg>
