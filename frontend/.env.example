# MITS Frontend — environment template
#
# Dev: leave as :4000 (matches `npm run dev` backend)
# Prod (Render): set to the backend service URL, e.g. https://mits-backend.onrender.com
VITE_API_URL=http://localhost:4000
